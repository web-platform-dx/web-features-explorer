<h3>Learning resources on MDN</h3>

{% if feature.mdnUrls.length > 0 %}
  <ul data-pagefind-body>
    {% for url in feature.mdnUrls %}
      <li>
        <a href="{{ url.url }}">
          {% if url.anchor %}
            {{ url.anchor | capitalize }}, in <em>{{ url.title }}</em>
          {% else %}
            {{ url.title }}
          {% endif %}
        </a>
      </li>
    {% endfor %}
  </ul>
{% else %}
  <strong>No MDN documentation found.</strong>
  {% if not feature.discouraged %}
    You can <a href="https://developer.mozilla.org/search?q={{ feature.name }}">search for the feature on MDN</a>. If you believe that MDN has no documentation about this feature, you can <a href="https://github.com/mdn/mdn/issues/new?template=content-or-feature-suggestion.yml">open an issue on MDN's GitHub repository</a>.
  {% endif %}
{% endif %}