{
  "type": "module",
  "name": "web-features-explorer",
  "description": "Visualize web features",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.5",
    "@11ty/eleventy-plugin-rss": "^3.0.0",
    "@mdn/browser-compat-data": "^8.0.0",
    "@schalkneethling/opengraph-cards-maker": "^0.1.1",
    "apexcharts": "^5.13.0",
    "browser-specs": "^4.77.0",
    "caniuse-lite": "^1.0.30001793",
    "glob": "^13.0.6",
    "pagefind": "^1.5.2",
    "playwright": "^1.60.0",
    "web-features": "next",
    "yaml": "^2.9.0"
  },
  "scripts": {
    "build": "npx rimraf docs && npx @11ty/eleventy && node generate-og-images.js",
    "setup:og-browser:ci": "npx playwright install --with-deps chromium",
    "serve": "npx @11ty/eleventy --serve",
    "pagefind": "npx pagefind --site docs --glob \"features/**/*.html\""
  }
}
